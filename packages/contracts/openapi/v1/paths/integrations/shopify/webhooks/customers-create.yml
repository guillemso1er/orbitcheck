tags:
  - Integrations
summary: Shopify customers/create webhook
description: Handles Shopify customers/create webhook events to sanitize and persist normalized customer addresses
operationId: shopifyCustomersCreateWebhook
security: []
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '../../../../components/schemas/ShopifyCustomer.yml'
responses:
  '200':
    description: Webhook processed successfully
  '400':
    description: Invalid webhook data
    content:
      application/json:
        schema:
          $ref: '../../../../components/schemas/Error.yml'
