tags:
  - Integrations
summary: Shopify customers/update webhook
description: Handles Shopify customers/update webhook events to refresh normalized customer addresses
operationId: shopifyCustomersUpdateWebhook
security: []
requestBody:
  required: true
  content:
    application/json:
      schema:
        type: object
        description: Shopify customer webhook payload
responses:
  '200':
    description: Webhook processed successfully
  '400':
    description: Invalid webhook data
    content:
      application/json:
        schema:
          $ref: '../../../../components/schemas/Error.yml'
