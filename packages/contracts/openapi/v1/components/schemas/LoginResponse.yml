type: object
properties:
  user:
    $ref: './User.yml'
  request_id:
    type: string
    description: Request identifier
  csrf_token:
    type: string
    description: CSRF token for subsequent requests
required:
  - user