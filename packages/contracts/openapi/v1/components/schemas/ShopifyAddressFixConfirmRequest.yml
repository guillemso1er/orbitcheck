type: object
properties:
  use_corrected:
    type: boolean
    description: Whether to use the corrected address (true) or keep original (false)
  shop_domain:
    type: string
    description: Shopify shop domain for verification
  address:
    $ref: './Address.yml'
required:
  - use_corrected
  - shop_domain
